class Downloader {
    static downloadPackage(url, pathToSaveTo) {
        console.log(`Downloading content package from ${url}...`);

        console.log("Download completed. (not really)");
    }
}

export default Downloader;